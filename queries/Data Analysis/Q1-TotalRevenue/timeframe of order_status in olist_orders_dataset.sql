-- What is the total revenue generated by Olist, and how has it changed over time?
SELECT 
	MIN(order_purchase_timestamp) AS start_date,
	MAX(order_purchase_timestamp) AS emd_date
FROM
	olist_orders_dataset
	